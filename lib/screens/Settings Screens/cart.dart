import 'package:flutter/material.dart';

class Caart extends StatelessWidget {
  const Caart({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Cart"),
    );
  }
}
